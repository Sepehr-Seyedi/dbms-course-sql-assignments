1 Question:

Retrieve a list of all customer names and all borrower names without duplicates.
(Use the UNION operator to combine both sets.)

2 Question:

Find the names of all people who are both customers and borrowers.
(Use the INTERSECT operator.)

3 Question:

Retrieve the names of all bank branches that appear in the loan table.
(Select the bname values from the loan table.)

4 Question

Retrieve the loan numbers (ln) for all loans issued by the TEH branch where the loan amount is greater than 2,000,000.
5 Question

Calculate the average account balance for each branch and return only
   the branches whose average balance is greater than 2,000,000,000.

 6 Question
 
 Write SQL code to design a banking database with tables for customers, accounts, depositors, borrowers, loans, and branches.
 Populate each table with sample data and then query to find the distinct names of depositors who have accounts in the TEH branch.
 
 7 Question
 
 Write SQL code to design a banking database with tables for customers, accounts, depositors, borrowers, loans, and branches.
 Populate each table with sample data and then query to display each borrower’s name, their loan number, and the loan amount using an INNER JOIN.
 
 8 Question
 
 Write SQL code to design a banking database with tables for customers, accounts, depositors, borrowers, loans, and branches.
 Populate each table with sample data and then query to display the names of borrowers, their loan numbers, and loan amounts for loans issued specifically from the TEH branch using an INNER JOIN.

